<%= form_with(model: member) do |form| %>
  <% if member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(member.errors.count, "error") %> prohibited this something from being saved:</h2>

      <ul>
        <% member.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :scoutnummer %>
    <%= form.text_field :scoutnummer %>
  </div>

  <div class="field">
    <%= form.label :voornaam %>
    <%= form.text_field :voornaam %>
  </div>

  <div class="field">
    <%= form.label :achternaam %>
    <%= form.text_field :achternaam %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>